# Netlify configuration for TanStack Start on Nitro's native Netlify preset

[build]
command = "npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "20"

[dev]
command = "npm run dev"
port = 3000
targetPort = 5173

[context.production]
command = "npm run build"
publish = "dist"
